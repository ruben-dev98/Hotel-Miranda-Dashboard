const Table = ({rows, columns}) => {
    
    /*const a = [
        {
            "label": "Guest",
            "property": row.fullname
        }
    ];*/

    return (
        <table>
            <thead>
            </thead>
            <tbody>
            </tbody>
        </table>
    );
}

export default Table;